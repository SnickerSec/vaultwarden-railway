# Archive Directory

This directory contains files that are no longer used but kept for reference.

## Files

- **Dockerfile.simple** - Simple Dockerfile variant (not used)
- **nixpacks.toml** - Nixpacks configuration (Railway uses Dockerfile instead)

These files are kept for historical reference but are not part of the active deployment.
