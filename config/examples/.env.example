# Domain Configuration
# IMPORTANT: Set this to your Railway domain (e.g., https://your-app.up.railway.app)
DOMAIN=https://your-app.up.railway.app

# Admin Panel
# Generate a secure token with: openssl rand -base64 48
ADMIN_TOKEN=your-secure-admin-token-here

# ============================================================================
# GOOGLE OAUTH AUTHENTICATION (Optional - requires setup)
# ============================================================================
# Follow GOOGLE_AUTH_SETUP.md for detailed instructions
# Uncomment and configure these to enable Google Auth protection

# Google OAuth Credentials (from Google Cloud Console)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth2 Proxy Cookie Secret (generate with: openssl rand -base64 32)
# OAUTH2_COOKIE_SECRET=your-cookie-secret-here

# Allowed Email Domains (restrict to specific domains or use * for all)
# Examples:
#   OAUTH2_EMAIL_DOMAINS=*                    # Allow all Google accounts
#   OAUTH2_EMAIL_DOMAINS=yourdomain.com       # Only @yourdomain.com
#   OAUTH2_EMAIL_DOMAINS=gmail.com,outlook.com # Multiple domains
# OAUTH2_EMAIL_DOMAINS=*

# Rocket Configuration
ROCKET_PORT=80
ROCKET_ADDRESS=0.0.0.0

# Database (Railway provides PostgreSQL)
# Railway will auto-inject DATABASE_URL if you add a PostgreSQL database
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Alternatively, use SQLite (default)
# DATA_FOLDER=/data

# Security Settings
SIGNUPS_ALLOWED=true
INVITATIONS_ALLOWED=true
SHOW_PASSWORD_HINT=false

# Email Configuration (Optional - for invite emails and 2FA)
# SMTP_HOST=smtp.gmail.com
# SMTP_FROM=your-email@gmail.com
# SMTP_PORT=587
# SMTP_SECURITY=starttls
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# WebSocket Support (for live sync)
WEBSOCKET_ENABLED=true

# Logging
LOG_LEVEL=info
EXTENDED_LOGGING=true

# Icon Service
ICON_SERVICE=internal

# Disable registration after creating your account (recommended)
# SIGNUPS_ALLOWED=false

# Push Notifications (Optional - requires configuration)
# PUSH_ENABLED=true
